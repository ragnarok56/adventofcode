# adventofcode

its a readme for https://adventofcode.com/

## setup 

Python
```bash
poetry install
poetry shell
```

Rust (inside each day directory that is a rust project)
```bash
cargo install
cargo run <args>
```